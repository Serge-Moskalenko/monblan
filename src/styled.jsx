import styled from 'styled-components';

const LoadButton = styled.button`
  display: block;
  margin: 20px auto;
  padding: 10px 20px;
  font-size: 16px;
  cursor: pointer;
`;

export default LoadButton